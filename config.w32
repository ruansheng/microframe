// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("microframe", "for microframe support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("microframe", "enable microframe support", "no");

if (PHP_MICROFRAME != "no") {
	EXTENSION("microframe", "microframe.c");
}

